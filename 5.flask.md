use python 3.9

### Install python libraries

copy requirements.txt

```
python -m venv env
env\Scripts\Activate.ps1
pip install -r requirements.txt
apache-airflow
 pip install -r resources\requirements.txt
```

## Start the prediction request Web Application

Set the `PROJECT_HOME` env variable with the path of you cloned repository, for example:

```

export PROJECT_HOME=/practica_creativa

```

Go to the `web` directory under `resources` and execute the flask web application file `predict_flask.py`:

```

cd practica_creativa/resources/web
python3 predict_flask.py

```

in docker compose I want to give access to practica_creativa/resources to the container and be able to reach/execute the flask app

go to
http://127.0.0.1:5001/flights/delays/predict_kafka

docker-compose down -v sparksubmit
docker-compose build --no-cache sparksubmit
docker-compose up -d --force-recreate sparksubmit

docker-compose up -d
docker-compose logs -f sparksubmit

docker-compose down -v
docker-compose build --no-cache flask
docker-compose up -d --force-recreate flask
docker-compose logs -f flask
